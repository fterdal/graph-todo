type TodoTask {
  id: Int!
  title: String
  text: String
  completed: Boolean
}

type User {
  id: Int!
  email: String!
}

type Query {
  TodoTaskById(id: Int!): TodoTask
  AllTodoTasks: [TodoTask]
  AllUsers: [User]
}
